<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rl.x.a21_inaction">

    <uses-permission android:name="android.permission.SET_ALARM" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".utils.SchedulerReceiver" />
        <receiver android:name=".utils.CounterReceiver"/>

        <activity android:name="._main.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".habit.view.NewHabitActivity" />
        <activity android:name=".add_task.view.AddTaskActivity" />
        <activity android:name=".add_expectation.view.AddExpectationActivity"></activity>

    </application>

</manifest>
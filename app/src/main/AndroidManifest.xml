<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.InAction.X.x21InAction">

    <uses-permission android:name="android.permission.SET_ALARM" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".utils.SchedulerReceiver" />
        <receiver android:name=".counter.receiver.CounterReceiver" />
        <receiver android:name=".counter.receiver.MidnightReceiver" />

        <activity android:name="._main.addExpectationView.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".intro_screens.addExpectationView.IntroActivity" />
        <activity android:name=".habit.addExpectationView.NewHabitActivity" />
        <activity
            android:name=".add_task.addExpectationView.AddTaskActivity"
            android:theme="@style/PoUpTheme" />
        <activity
            android:name=".temp_expectation.addExpectationView.AddExpectationActivity"
            android:theme="@style/PoUpTheme"></activity>

    </application>

</manifest>